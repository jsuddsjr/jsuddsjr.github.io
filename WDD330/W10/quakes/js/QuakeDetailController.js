import Quake from "Quake.js";
import QuakesView from "QuakesView.js";

export default class QuakeDetailController {
  constructor(parentSelector) {
    this.parentSelector = parentSelector;
  }

  init(id) {}
}
